import { LogoDirectivaDirective } from './logo-directiva.directive';

describe('LogoDirectivaDirective', () => {
  it('should create an instance', () => {
    const directive = new LogoDirectivaDirective();
    expect(directive).toBeTruthy();
  });
});
